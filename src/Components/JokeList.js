import React from "react";

const JokeList = (props) => (
    <div className="list">
        <h3>{props.title}</h3>
        <ul>
            <li>

            </li>
        </ul>
    </div>
);

export default JokeList;
